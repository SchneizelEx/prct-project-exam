<?php
// คัดลอกไฟล์นี้เป็น config.php แล้วใส่ค่าจริงของเซิร์ฟเวอร์
// อย่า commit config.php (มีอยู่ใน .gitignore แล้ว)

return [
    'db' => [
        'host' => '127.0.0.1',
        'port' => '3306',
        'name' => 'present_registration',
        'user' => 'root',
        'pass' => '',
        'charset' => 'utf8mb4',
    ],
    'app' => [
        // ใช้เป็น base สำหรับ session cookie / รหัสลับ csrf
        'timezone' => 'Asia/Bangkok',
        // ถ้า deploy ไว้ที่ sub-path ของโดเมน (เช่น https://www.prcs.ac.th/project-exam/)
        // ให้ใส่ '/project-exam' — ถ้า deploy ที่ root ของโดเมนให้เว้นว่าง ''
        'base_path' => '',
        // โดเมนที่ถูกต้องเพียงหนึ่งเดียว (ไม่ต้องมี https://) เช่น 'www.prcs.ac.th'
        // ตั้งค่านี้เมื่อเซิร์ฟเวอร์ตอบเว็บเดียวกันได้จากหลายโดเมน/สคีม (มี www / ไม่มี www / http / https)
        // ระบบจะ redirect ไปโดเมนนี้ (https เสมอ) ให้อัตโนมัติก่อนเริ่ม session ทุกครั้ง
        // ป้องกัน session คนละก้อนกันเวลาผู้ใช้เข้าคนละโดเมนกัน (ทำให้ CSRF token ไม่ตรงกัน)
        // เว้นว่างไว้ตอนพัฒนา/ทดสอบในเครื่อง (localhost)
        'canonical_host' => '',
    ],
    'upload' => [
        // path นี้ต้องอยู่นอก public/ document root
        'storage_dir' => __DIR__ . '/../storage/uploads',
        'max_size_bytes' => 25 * 1024 * 1024, // 25MB
    ],
];
